name: DevSecOps CI/CD Pipeline

on:
  push:
    branches:
      - dev
      - main

jobs:
  build-and-secure:
    runs-on: ubuntu-latest

    steps:
    # ✅ Checkout FULL repo (IMPORTANT FIX)
    - name: Checkout Code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0   # 🔥 Enables full history scan

    # 🐳 Build Docker Image
    - name: Build Docker Image
      run: |
        cd app
        docker build -t devsecops-app .

    # 🔐 Gitleaks Full Repository Scan (FINAL FIX)
    - name: Run Gitleaks Scan
      run: |
        docker run --rm -v $(pwd):/repo zricethezav/gitleaks detect \
          --source=/repo \
          --no-git \
          --verbose \
          --redact \
          --exit-code=1

    # 🛡 Trivy Vulnerability Scan
    - name: Run Trivy Scan
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: devsecops-app
        format: table
        exit-code: 1
        severity: CRITICAL,HIGH

    # ✅ Success Message
    - name: Pipeline Success
      if: success()
      run: echo "✅ Build and Security Checks Passed!"